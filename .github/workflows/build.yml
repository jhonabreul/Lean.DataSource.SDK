name: Build & Test

on:
  push:
    branches: ['*']
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Free space
        run: df -h && rm -rf /opt/hostedtoolcache* && df -h

      - name: Pull Foundation Image
        uses: addnab/docker-run-action@v3
        with:
          image: quantconnect/lean:foundation

      - name: Checkout Lean Same Branch
        id: lean-same-branch
        uses: actions/checkout@v2
        continue-on-error: true
        with:
          ref: ${{ github.ref }}
          repository: QuantConnect/Lean
          path: Lean

      - name: Checkout Lean Master
        if: steps.lean-same-branch.outcome != 'success'
        uses: actions/checkout@v2
        with:
          repository: QuantConnect/Lean
          path: Lean

      - name: Move Lean
        run: mv Lean ../Lean

      - name: BuildDataSource
        run: dotnet build ./QuantConnect.DataSource.MyCustom.csproj /p:Configuration=Release /v:quiet /p:WarningLevel=1

      - name: BuildTests
        run: dotnet build ./QuantConnect.DataSource.MyCustom.Tests/QuantConnect.DataSource.MyCustom.Tests.csproj /p:Configuration=Release /v:quiet /p:WarningLevel=1

      - name: Run Tests
        run: dotnet test ./QuantConnect.DataSource.MyCustom.Tests/bin/Release/net6.0/Tests.dll
